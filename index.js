function beforeEach(){
    spy = sinon.spy(console,`log`);
}
function afterEach(){
    spy.restore();
}
function writeCards([]){
    for (let spy = 0; spy < array.length; spy++) {
        const element = array[spy];
        
    }return (writeCards(`Thank you,`[writeCards]`,for the wonderful surprise gift!`))   
}
function afterEach(countDown){
    afterEach(countDown(10)).console.log(spy.callCount);;
    countDown.spy.calledWithExactly(4)
}